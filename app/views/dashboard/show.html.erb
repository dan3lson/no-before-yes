<%= render 'shared/page_title',
           page_title: 'Dashboard | Track weekly progress and complete action items' %>

<%= render 'onboarding/finished_alert', finished: @onboarding_finished %>

<%= render 'snapshot', dashboard: @dashboard %>

<%= render 'overdue_contacts', dashboard: @dashboard %>

<%= render 'follow_up_contacts', dashboard: @dashboard %>

<%= render 'untouched_contacts', dashboard: @dashboard %>
