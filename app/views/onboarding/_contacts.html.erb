<section id="contacts">
  <h1>Contacts</h1>
  <p class="lead">An online roladex to organize your customer base.</p>

  <% if current_user.contacts? %>
    <p>
      Contacts are the foundation of No Before Yes: build your relationship with
      them and see how it evolves over time.
    </p>

    <%= render 'next_button', next_step: :touchpoints %>
  <% else %>
    <p>
      Let's create your first contact &mdash; this is the beginning of your
      customer base. Don’t worry, you can create as many contacts as you want
      later.
    </p>

    <%= render 'contacts/form',
               model: [:onboarding, contact],
               contact: contact,
               local: false %>
  <% end %>
</section>
