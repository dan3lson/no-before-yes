<%= form_with(model: touchpoint, local: true) do |form| %>
  <% if touchpoint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(touchpoint.errors.count, "error") %> prohibited this touchpoint from being saved:</h2>

      <ul>
        <% touchpoint.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :recap %>
    <%= form.text_area :recap %>
  </div>

  <div class="field">
    <%= form.label :follow_up_on %>
    <%= form.date_select :follow_up_on, { order: [:month, :day, :year] } %>
  </div>

  <div class="field">
    <%= form.label :source %>
    <%= form.text_field :source %>
  </div>

  <div class="field">
    <%= form.label :result %>
    <%= form.text_field :result %>
  </div>

  <div class="field">
    <%= form.label :contact_id %>
    <%= form.collection_select :contact_id, contacts, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
