<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>

  <body>
    <%= render 'layouts/navbar/navbar' %>

    <div class="container">
      <%= render 'onboarding/unfinished_alert' %>
      <%= render 'layouts/flashes' %>

      <%= yield %>
    </div>
  </body>
</html>
